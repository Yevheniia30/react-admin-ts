import React, { FC } from "react";

interface CommentsProps {
  width: string;
  height: string;
}

const Comments: FC<CommentsProps> = ({ width, height }) => {
  return <ul></ul>;
};

export default Comments;
